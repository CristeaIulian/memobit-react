export interface CountryPhonePrefix_ {
	c_: string; // country code
	n_: string; // name
	p_: string; // prefix
	m_: string; // mask
	l_: number; // location (pixels)
	h_?: number; // height (pixels)
}
