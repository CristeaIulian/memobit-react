export const compactNumberFormatter: Intl.NumberFormat = new Intl.NumberFormat('en-US', {
	notation: 'compact',
});

export const separatorsNumberFormatter: Intl.NumberFormat = new Intl.NumberFormat('en-US');
